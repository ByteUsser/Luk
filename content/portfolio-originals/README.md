# Oryginały i pliki robocze portfolio

Ten folder przechowuje cięższe pliki źródłowe przeniesione z `public/portfolio`.
Nie są serwowane bezpośrednio jako assety strony.

Na stronie używaj zoptymalizowanych plików z `public/portfolio` albo pełnego workflow galerii:

```bash
npm run gallery:build
```
